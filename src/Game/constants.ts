export const N = 50;
export const DEPTH = Math.ceil(Math.log2(N * N));

export const UP = 1n;
export const LEFT = 2n;
export const RIGHT = 3n;
export const DOWN = 4n;

export const STEP = {
  UP,
  LEFT,
  RIGHT,
  DOWN,
};
